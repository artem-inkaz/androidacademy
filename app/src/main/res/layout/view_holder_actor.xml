<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="80dp"
    android:layout_height="110dp"
    android:layout_marginStart="16dp"
    android:layout_marginTop="16dp"
    android:background="@color/background_movies_list">

    <ImageView
        android:id="@+id/imgViewActors"
        android:layout_width="80dp"
        android:layout_height="80dp"

        app:srcCompat="@android:drawable/sym_def_app_icon" />

    <TextView
        android:id="@+id/txtViewActorName"
        android:layout_width="80dp"
        android:layout_height="wrap_content"

        android:layout_gravity="bottom"

        android:layout_marginTop="0dp"
        android:contentDescription="@string/descriptions"
        android:fontFamily="@font/roboto_bold"
        android:text="ActorsFullName"
        android:textColor="@color/fotos_description"
        android:textSize="12sp" />

</FrameLayout>